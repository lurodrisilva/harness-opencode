# Lint matrix: bring-your-own auth + provider Secrets.
# Both secret-*.yaml templates skip rendering when existingSecret is set.
auth:
  existingSecret: prebuilt-auth
providers:
  existingSecret: prebuilt-providers
